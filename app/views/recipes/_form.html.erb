<%=form_for recipe do |r|%>
<%= r.label :name%>
<%= r.text_field :name%><br>
<%= r.collection_check_boxes :ingredient_ids, Ingredient.all, :id, :name do |box|%>
<%= box.check_box %>
<%= box.label%><br>
<%end%>
<%= r.submit%>
<%end%>